# 医疗需求文档总结

## 🧑‍💻 User


你帮我看一下，这些需求应该怎么写文档，把每一个需求对应的产品指导文档独立输出出来

---
## 🤖 Gemini 3 Pro Preview

<div style="border: 2px solid #dddddd; border-radius: 10px;">
  <details style="padding: 5px;">
    <summary>已深度思考</summary>
    **Refining Documentation Goals**<br><br>I'm now zeroing in on the request, pinpointing the community manager's non-profit focus and need for "Vibe Coding" documentation. I'm visualizing the desired output: clear, actionable documentation tailored to this specific application. This is shaping up nicely!<br><br><br>**Developing a Framework**<br><br>I've got a firm grasp on the target audience: non-technical users leveraging AI coding aids. The 'Micro-light Matrix' will be my core input. To proceed efficiently, I'll adopt the persona of a 'Vibe Coding Architect'. This will allow me to apply the 'OutputFormat' systematically. Key constraints are in place, so it will be JavaScript-first, and I'll keep Python to a minimum. To manage the scope, I will create templates that are representative of the full range of app ideas.<br><br><br>**Creating Document Structure**<br><br>I'm now drafting a detailed outline for the documentation, starting with an introduction that highlights the value of the "Vibe Coding" approach. I will then work on the core component, the template design that I've decided to call the "Golden Standard". Next, I'll go through category 1: "The Decoder" focusing on the "Report Translator" app idea. Finally, I will write about category 2: "The Tracker", with the "Pain Logger" app idea.<br><br><br>**Outlining New Use Cases**<br><br>I've just added three new app ideas to the documentation: a "Can I Eat This?" food query app, a "Reimbursement Calc" tool, and a "The Tree Hole" anxiety chatbot. Each will cover a different category, each with a different focus on UI interactions. I'm prioritizing the prompts and tech stacks for each, with the CBT chatbot built with Vercel's SDK and AI Stream. I’m thinking that "Report Translator" is tricky, so I will rewrite it to be clearer.<br><br><br>**Optimizing Template Examples**<br><br>I'm now revising my template examples to accommodate simpler technologies. Instead of using complex OCR for the "Stool Analyzer," I'll advise direct use of LLM vision models to reduce complexity. I'm removing that example due to possible safety filters. I'm focusing on "Pain Logger" now as a cleaner template. I'm also working to make sure all code blocks can be copy-pasted directly by the end-user.<br><br><br>**Generating Additional Examples**<br><br>I've just added two new app ideas to the documentation to flesh out the template collection. These include a "Trend Diary" app and a "Traffic Light Diet Assistant". These were added to ensure the templates are diverse, showcasing different UI and technology selections. I'm double-checking the templates, ensuring that the JavaScript-first requirement is met. I'm also confirming the prompt structure is set to be usable by AI agents.<br><br><br>
  </details>
</div>

这是一个非常棒的战略规划。作为 Vibe Coding 架构师，我非常认同**“Prompt as App”**和**“原子化开发”**的理念。对于非技术背景的病友和医护人员，我们需要将“模糊的想法”转化为“AI 能够立即执行的代码蓝图”。

为了方便您的社区成员（无论是医生、家属还是志愿者）直接上手，我从您提供的矩阵中挑选了 **5 个最具代表性的高频刚需应用**（覆盖了五个板块），并将它们转化为标准化的 **Vibe Coding 产品指导文档 (PRD)**。

**使用方法：**
您可以直接将下面的每一个 Markdown 块复制出来，保存为单独的 `.md` 文件（例如 `需求_报告翻译器.md`），然后直接发给 Cursor、Windsurf 或 Bolt.new，即可开始自动编程。

---

### 📂 通用模板说明
在此之前，我为您提炼了一个**“Vibe Coding 标准需求模板”**。如果您后续要整理其他 195 个应用，只需填空即可：

```markdown
# [应用名]
## 1. 核心故事
谁，在什么情况下，解决什么痛点。
## 2. MVP 功能范围
输入 -> 处理 -> 输出。
## 3. 技术栈 (JS-First)
Next.js + Tailwind + Shadcn UI + (核心逻辑库)。
## 4. 逻辑步骤
1. 初始化。2. UI。3. 逻辑。
## 5. AI Prompt
给 AI 的直接指令。
```

---

以下是为您精选的 5 个独立需求文档：

### 1. 【初诊】小白报告解读器 (Report Translator)

```markdown
# 应用名称: 小白报告解读器 (Medical Report Decoder)

## 1. 项目概述 (User Story)
> 刚确诊的患者或家属拿到满是专业术语（如 Ki-67, CA19-9, EGFR）的检查报告时，感到恐慌且无法理解。本工具允许用户上传报告照片或输入文本，AI 将其翻译为“小学三年级水平”的大白话比喻。

## 2. 核心价值与功能 (MVP Scope)
- **输入**: 
  - 文本输入框（粘贴报告段落）。
  - (进阶) 图片上传（调用 LLM 的 Vision 能力）。
- **处理**: 
  - 识别医学术语。
  - 调用 AI 接口，要求使用“比喻”手法解释（例如：白细胞是身体的警察）。
  - 提取关键异常指标（高/低）。
- **输出**: 
  - 结构化的解读卡片：[指标名] - [通俗解释] - [临床意义]。
- **不做**: 不做诊断，不做治疗建议，必须在底部显著位置显示免责声明。

## 3. 技术栈选型 (JS-First Stack)
- **Framework**: Next.js 14 (App Router)
- **Styling**: Tailwind CSS
- **AI Integration**: Vercel AI SDK (接入 OpenAI/Claude/DeepSeek)
- **Components**: Shadcn UI (Textarea, Card, Button, Alert)

## 4. 核心逻辑与数据流
1.  **初始化**: 创建 Next.js 项目，配置 API Key 环境变量。
2.  **界面**: 一个简洁的文本框/上传区，下方是结果展示区。
3.  **逻辑**: 
    - 用户提交文本 -> Server Action 调用 LLM。
    - System Prompt 设定为：“你是一个友善的医生朋友，请用极其通俗的比喻解释以下医学指标，不要使用专业术语堆砌。”
4.  **渲染**: 将 AI 返回的 Markdown 文本渲染在卡片中。

## 5. 给 AI IDE 的提示词 (Meta-Prompt)
> "你好，我要开发一个'小白报告解读器'。
> 1. 请使用 Next.js 14 + Tailwind CSS + Shadcn UI 初始化项目。
> 2. 页面包含一个大的文本输入框，用户粘贴医学报告内容。
> 3. 点击'解读'后，调用 Vercel AI SDK (使用 mock 数据或 OpenAI 接口) 进行流式输出。
> 4. System Prompt 核心逻辑：将医学术语转化为生活中的比喻（如汽车、房屋、战争）。
> 5. 必须在页面底部添加醒目的红色警告组件：'结果仅供参考，不作为医疗诊断依据'。
> 请先构建 UI，然后编写调用 AI 的逻辑。"
```

---

### 2. 【治疗】痛感趋势日记 (Pain Logger)

```markdown
# 应用名称: 痛感趋势日记 (Pain Logger)

## 1. 项目概述 (User Story)
> 癌症患者的疼痛管理需要精确的数据支持。患者往往在复诊时忘记过去一周的疼痛频率。本工具提供极简界面，让患者只需“点三下”即可记录疼痛，并自动生成趋势图供医生参考。

## 2. 核心价值与功能 (MVP Scope)
- **输入**: 
  - 疼痛等级 (0-10 VAS评分滑块)。
  - 疼痛部位 (简单的标签选择：头、胸、腹、骨)。
  - 服药情况 (是/否)。
- **处理**: 
  - 将数据保存到浏览器 LocalStorage (保护隐私，无需后端)。
  - 计算最近 7 天的平均痛感。
- **输出**: 
  - 一个折线图，显示痛感随时间的变化。
  - 一个“医生视图”按钮，点击后生成纯文本简报。
- **不做**: 不做复杂的账户系统，数据存本地即可。

## 3. 技术栈选型 (JS-First Stack)
- **Framework**: Next.js 14 or React (Vite)
- **Styling**: Tailwind CSS
- **Visualization**: Recharts (轻量级图表库)
- **Storage**: LocalStorage (原生 API)
- **Components**: Shadcn UI (Slider, Badge, Button)

## 4. 核心逻辑与数据流
1.  **项目初始化**: 搭建单页应用。
2.  **界面交互**: 
    - 巨大的滑块 (Slider) 选择 0-10。
    - 提交按钮。
    - 历史记录列表。
3.  **逻辑处理**: 
    - `useEffect` 读取 LocalStorage 中的 `painLogs` 数组。
    - 新增记录时 `push` 到数组并保存。
4.  **结果渲染**: 使用 `Recharts` 渲染 `<LineChart>`，X轴为时间，Y轴为等级。

## 5. 给 AI IDE 的提示词 (Meta-Prompt)
> "你好，我要开发一个'痛感趋势日记' Web App。
> 1. 技术栈：Next.js + Tailwind + Shadcn UI + Recharts。
> 2. 核心功能：
>    - 首页有一个大滑块 (0-10)，颜色随数值从绿变红。
>    - 点击'记录'按钮，保存当前时间戳和数值到 LocalStorage。
>    - 首页下方展示一个折线图，读取 LocalStorage 数据展示最近 7 天趋势。
> 3. 不需要后端数据库，所有数据存储在浏览器端。
> 4. 请确保移动端适配良好，按钮要大，方便老年人操作。
> 请开始编写代码。"
```

---

### 3. 【饮食】能吃吗？红绿灯助手 (Can I Eat This?)

```markdown
# 应用名称: 红绿灯饮食助手 (Can I Eat This?)

## 1. 项目概述 (User Story)
> 患者经常问“我能吃海参吗？”“我能吃糖吗？”。本工具结合患者的基础病（如糖尿病、高血压、化疗中），对特定食物给出“红灯（禁食）、黄灯（少吃）、绿灯（放心吃）”的建议。

## 2. 核心价值与功能 (MVP Scope)
- **输入**: 
  - 患者标签（多选：化疗中、糖尿病、高血压、腹泻中）。
  - 查询食物（输入框：如“红烧肉”）。
- **处理**: 
  - AI 根据【食物成分】+【患者标签】进行逻辑判断。
  - 例如：红烧肉(高脂) + 腹泻中 = 红灯。
- **输出**: 
  - 醒目的红/黄/绿卡片。
  - 简短理由（一句话）。
- **不做**: 不做精确的卡路里计算，只做定性判断。

## 3. 技术栈选型 (JS-First Stack)
- **Framework**: Next.js 14
- **Styling**: Tailwind CSS + Lucide Icons (Traffic light icons)
- **Logic**: Vercel AI SDK (Prompt Engineering)
- **Components**: Shadcn UI (Toggle Group, Input, Card)

## 4. 核心逻辑与数据流
1.  **初始化**: Next.js 项目。
2.  **界面**: 
    - 顶部：状态选择器（Toggle Group）。
    - 中部：搜索框。
    - 底部：结果展示区。
3.  **逻辑**: 
    - 构造 Prompt: `User conditions: [${conditions}], Food: [${food}]. Assess safety. Output JSON: {status: 'red'|'yellow'|'green', reason: '...'}`.
4.  **渲染**: 根据 status 渲染不同颜色的背景块。

## 5. 给 AI IDE 的提示词 (Meta-Prompt)
> "你好，我要开发一个'癌症饮食红绿灯'应用。
> 1. 使用 Next.js + Tailwind。
> 2. UI 包含两部分：
>    - 第一步：用户选择身体状况标签（如：正在化疗、糖尿病、白细胞低）。
>    - 第二步：用户输入食物名称。
> 3. 逻辑：调用 AI 接口，根据身体状况和食物，返回 'Red', 'Yellow', 'Green' 三种状态及原因。
> 4. UI 反馈：如果是 Red，显示红色背景和禁止图标；Green 显示绿色背景和笑脸。
> 5. 请使用 Mock AI 函数模拟返回结果，或者预留 OpenAI API 调用位置。
> 请开始编码。"
```

---

### 4. 【经济】医保报销计算器 (Reimbursement Calc)

```markdown
# 应用名称: 医保报销计算器 (Reimbursement Calc)

## 1. 项目概述 (User Story)
> 治疗费用复杂，患者难以预估自费部分。本工具通过简单的表单，帮助患者计算在特定报销比例下的预估自费金额，特别是针对“双通道”或“门特”药物。

## 2. 核心价值与功能 (MVP Scope)
- **输入**: 
  - 药物/检查总费用。
  - 报销比例 (提供常见档位：50%, 70%, 90% 或 自定义)。
  - 起付线金额。
- **处理**: 
  - 纯数学计算：`Max(0, (总费用 - 起付线) * (1 - 报销比例))`。
- **输出**: 
  - 预估报销金额。
  - 预估自费金额。
  - 可视化饼图（自费 vs 报销）。
- **不做**: 不做自动联网查询医保政策（数据太复杂），只做计算器。

## 3. 技术栈选型 (JS-First Stack)
- **Framework**: 单个 HTML 文件 或 Next.js
- **Styling**: Tailwind CSS
- **Logic**: Vanilla JS (原生数学运算)
- **Visualization**: Chart.js 或 简单的 CSS 进度条

## 4. 核心逻辑与数据流
1.  **初始化**: 简单的 React 组件。
2.  **界面**: 三个输入框（总额、起付线、比例）。
3.  **逻辑**: 
    - 使用 `useState` 监听输入变化。
    - 实时计算结果，无需点击按钮。
4.  **渲染**: 数字动态跳动效果，下方显示一个简单的比例条。

## 5. 给 AI IDE 的提示词 (Meta-Prompt)
> "你好，我要做一个'极简医保计算器'。
> 1. 技术栈：React + Tailwind CSS。
> 2. 界面上有三个输入框：'医疗总费用'、'起付线金额'、'报销比例(%)'。
> 3. 实时计算两个数值：'预计报销' 和 '预计自费'。
> 4. 核心算法：(总费用 - 起付线) * 比例 = 报销额。请注意处理负数情况（总费用低于起付线）。
> 5. 视觉上，用一个双色进度条直观展示自费与报销的比例。
> 6. 代码要尽可能简单，最好是一个单独的组件文件。
> 请开始编写。"
```

---

### 5. 【心理】焦虑树洞 (The Tree Hole)

```markdown
# 应用名称: 焦虑树洞 (The Tree Hole)

## 1. 项目概述 (User Story)
> 癌症患者或家属常有无法对亲人说的恐惧。这个工具提供一个绝对安全、无评判的 AI 倾听者，它不给建议，只负责共情和倾听，缓解深夜的焦虑。

## 2. 核心价值与功能 (MVP Scope)
- **输入**: 语音输入（Web Speech API）或 文字聊天。
- **处理**: 
  - AI 角色设定为“拥有丰富安宁疗护经验的倾听者”。
  - 严格限制 AI 不给医疗建议，只使用“共情、确认、开放式提问”技巧。
- **输出**: 
  - 温暖、简短的文字回复。
- **不做**: 不做复杂的历史记录存储（阅后即焚，减轻心理负担）。

## 3. 技术栈选型 (JS-First Stack)
- **Framework**: Next.js 14
- **Styling**: Tailwind CSS (暖色调，深色模式)
- **AI**: Vercel AI SDK (`useChat` hook)
- **Components**: Shadcn UI (ScrollArea, Avatar)

## 4. 核心逻辑与数据流
1.  **初始化**: Next.js AI Chat 模板。
2.  **界面**: 类似微信的聊天窗口，但背景色温暖（如米色或深蓝）。
3.  **逻辑**: 
    - System Prompt 设定：你不是医生，你是树洞。不要给解决方案，要说“我听到你很难过...”，“这一定很不容易...”。
4.  **渲染**: 流式输出文字。

## 5. 给 AI IDE 的提示词 (Meta-Prompt)
> "你好，我要开发一个名为'焦虑树洞'的 AI 聊天应用。
> 1. 使用 Next.js + Vercel AI SDK。
> 2. 界面风格：宁静、温暖、极简。去除多余的菜单，只有一个对话框。
> 3. 核心 System Prompt：'你是一个只倾听不评判的树洞。用户是癌症患者或家属。无论他们说什么，请用共情的方式回应，肯定他们的情绪。严禁给出医疗建议。回复要简短温暖。'
> 4. 不需要保存聊天记录，刷新页面即重置，保护用户隐私。
> 5. 请提供完整的代码结构。
> 请开始编写。"
```
